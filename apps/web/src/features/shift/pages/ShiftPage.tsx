import { PlaceholderPage } from "@/shared/components/PlaceholderPage";

export const ShiftPage = () => {
  return (
    <PlaceholderPage
      description="Create and manage reusable shift templates for each branch."
      title="Shift Templates"
    />
  );
};
